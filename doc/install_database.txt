
apt install mariadb-server libmariadbclient-dev python3-venv build-essential python3-dev
ln -s mariadb_config /usr/bin/mysql_config

python3 -m venv venv/
source venv/bin/activate
pip3 install -r requirements/requirements.txt


create database das character set UTF8 collate utf8_bin;
CREATE USER 'das'@'localhost' IDENTIFIED BY 'xxxxxxxx';
GRANT ALL PRIVILEGES ON `das` . * TO 'das'@'localhost';
flush privileges;


